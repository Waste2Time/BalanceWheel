# 流程编排模块

## 已完成
- 端到端管线：检查/读取缓存数据，不存在时用 `SyntheticSource` 生成并校验；随后运行 baseline 与 variant 策略并输出报告。
- 可配置入口：通过 `run_pipeline` 函数传入标的、时间区间与策略参数。
- 简要日志：在关键阶段输出信息，便于排错。

## 必须完成但未完成
- 命令行/配置文件驱动的入口脚本。
- 数据源选择与多源交叉验证的管线化实现。
- 多实验批量运行与结果存档。

## 可选项
- UI 化的运行向导与参数表单。
- 与调度系统对接（定时回测、自动报表推送）。
