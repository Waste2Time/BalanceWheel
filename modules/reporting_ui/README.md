# 报告与可视化模块

## 已完成
- 报告生成器：将回测指标与交易统计汇总为文本报告。
- 图表工具：使用 matplotlib（可选）生成收益曲线与回撤曲线；在缺少依赖时优雅降级。
- 支持实验对比：绘制 baseline 与 variant 的对比曲线（若传入多条结果）。

## 必须完成但未完成
- 交互式可视化（Plotly/Bokeh）与轻量 Web UI。
- 更丰富的图表（分布、月度收益、仓位变化）。
- 报告导出到 HTML/PDF，附带图片与表格。

## 可选项
- 与实验管理集成，提供运行历史的可视化面板。
- 将图表上传至远端存储并生成分享链接。
